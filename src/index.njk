---
layout: base.njk
title: Home
---
<section class="hero">
  <div class="ornament ornament--top">{% include "ornament.njk" %}</div>
  <h1>Hi, I’m {{ site.shortName }} — AWS DevOps Engineer.</h1>
  <p>Automation, reliability, and practical writing. This is my portfolio of
    <a href="{{ (site.base or '') }}/blog/">blogs</a>,
    <a href="{{ (site.base or '') }}/projects/">projects</a>,
    and <a href="{{ (site.base or '') }}/achievements/">achievements</a>.
  </p>
  <div class="ornament ornament--bottom">{% include "ornament.njk" %}</div>
</section>

<section>
  <h2>Latest posts</h2>
  <ul class="list">
    {% for post in collections.posts %}
      {% if loop.index0 < 5 %}
        <li>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
          <span class="kicker">— {{ post.date | date("MMM dd, yyyy") }}</span>
        </li>
      {% endif %}
    {% endfor %}
  </ul>
</section>

<section>
  <h2>Featured projects</h2>
  <div class="grid">
    {% for p in collections.projects %}
      {% if loop.index0 < 3 %}
        {% set title = p.data.title %}
        {% set url = p.url %}
        {% set description = p.data.description %}
        {% set tags = p.data.tags %}
        {% set category = "project" %}
        {% set repo = p.data.repo %}
        {% include "card.njk" %}
      {% endif %}
    {% endfor %}
  </div>
</section>
